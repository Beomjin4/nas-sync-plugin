{
  "name": "nas-sync",
  "version": "0.1.0",
  "description": "Obsidian plugin for self-hosted NAS sync.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit && node esbuild.config.mjs production"
  },
  "keywords": ["obsidian", "sync", "nas"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.12.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.21.0",
    "obsidian": "^1.6.0",
    "tslib": "^2.6.0",
    "typescript": "^5.4.0"
  }
}
